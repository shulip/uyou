spring:
  datasource:
    url: jdbc:mysql:///uyou?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username:
    password:
server:
  port: 8080
mybatis:
  type-aliases-package: com.uyou.entity
  mapper-locations: classpath:mybatis/mapper/*.xml,classpath:mybatis/helperMapper/*.xml
  config-location: classpath:mybatis/mybatis-config.xml
jasypt:
  encryptor:
    password: WwDih=dDq)
